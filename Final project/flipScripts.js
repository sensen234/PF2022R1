const cards = document.querySelectorAll('.cardElements');
var card = cards.forEach

function flipCard() {
    console.log('clicked');
};

//cards.forEach(function(card){
//    card.addEventlistener('click', flipCard
// })
//cards.forEach(card => card.addEventListener('click', flipCard));